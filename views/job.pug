extends layout
block content
  a(href='/') Main Page
  h2 Job Detail
  p #{job.title}

  h3 Job Information

  <em>Title:</em> #{job.title}
  br
  <em>Content:</em> #{job.content}
  br
  <em>Price:</em> #{job.price}
  br
  <em>Delivery Time:</em> #{job.deliveryTime}
  br
  <em>Freelancer: </em>
    a(href=`/freelancers/${job.freelancer._id}`) #{job.freelancer.name}
  br
  <em>Rating:</em>
    if job.comments.length
      ul
        each comment in job.comments
          li Rating: #{comment.rating} | Comment: #{comment.text}
    else
      p This job doesn't have any comments yet.
